name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-22.04 # Вказуємо конкретну версію Ubuntu для більшої передбачуваності
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3 # Оновлюємо до останньої версії

      - name: Install and build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist
